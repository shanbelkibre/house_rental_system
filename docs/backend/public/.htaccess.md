# .htaccess

## Code (numbered)

```text
1 <IfModule mod_rewrite.c>
2     <IfModule mod_negotiation.c>
3         Options -MultiViews -Indexes
4     </IfModule>
5 
6     RewriteEngine On
7 
8     # Handle Authorization Header
9     RewriteCond %{HTTP:Authorization} .
10     RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
11 
12     # Handle X-XSRF-Token Header
13     RewriteCond %{HTTP:x-xsrf-token} .
14     RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]
15 
16     # Redirect Trailing Slashes If Not A Folder...
17     RewriteCond %{REQUEST_FILENAME} !-d
18     RewriteCond %{REQUEST_URI} (.+)/$
19     RewriteRule ^ %1 [L,R=301]
20 
21     # Send Requests To Front Controller...
22     RewriteCond %{REQUEST_FILENAME} !-d
23     RewriteCond %{REQUEST_FILENAME} !-f
24     RewriteRule ^ index.php [L]
25 </IfModule>
```

## Line-by-line explanation

- Line 1: Implements application logic: <IfModule mod_rewrite.c>
- Line 2: Implements application logic: <IfModule mod_negotiation.c>
- Line 3: Implements application logic: Options -MultiViews -Indexes
- Line 4: Implements application logic: </IfModule>
- Line 5: Blank line for separation.
- Line 6: Implements application logic: RewriteEngine On
- Line 7: Blank line for separation.
- Line 8: Implements application logic: # Handle Authorization Header
- Line 9: Implements application logic: RewriteCond %{HTTP:Authorization} .
- Line 10: Implements application logic: RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
- Line 11: Blank line for separation.
- Line 12: Implements application logic: # Handle X-XSRF-Token Header
- Line 13: Implements application logic: RewriteCond %{HTTP:x-xsrf-token} .
- Line 14: Implements application logic: RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]
- Line 15: Blank line for separation.
- Line 16: Implements application logic: # Redirect Trailing Slashes If Not A Folder...
- Line 17: Implements application logic: RewriteCond %{REQUEST_FILENAME} !-d
- Line 18: Implements application logic: RewriteCond %{REQUEST_URI} (.+)/$
- Line 19: Implements application logic: RewriteRule ^ %1 [L,R=301]
- Line 20: Blank line for separation.
- Line 21: Implements application logic: # Send Requests To Front Controller...
- Line 22: Implements application logic: RewriteCond %{REQUEST_FILENAME} !-d
- Line 23: Implements application logic: RewriteCond %{REQUEST_FILENAME} !-f
- Line 24: Implements application logic: RewriteRule ^ index.php [L]
- Line 25: Implements application logic: </IfModule>
